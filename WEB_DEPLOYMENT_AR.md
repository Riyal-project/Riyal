# نشر تطبيق ريال على Vercel

هذا المشروع هو تطبيق Flutter فقط. موقع التعريف الذي يديره صديقك مشروع مستقل ولا يحتاج أي تعديل هنا.

## النشر الحالي

- رابط التطبيق الثابت: **https://riyal-web.vercel.app**
- مشروع Vercel: **riyal-web** داخل حساب **Riyal (`riyal3`)**.
- لوحة المشروع: https://vercel.com/riyal3/riyal-web
- نُشرت نسخة الإنتاج في 20 سبتمبر 2026 من `build/vercel`.
- مفتاح Gemini والنموذج محفوظان كمتغيرات خادم سرية في Production وPreview.
- حماية Vercel Firewall مفعلة: 10 طلبات في الدقيقة لكل عنوان IP لمسار `/api/gemini`.
- GitHub غير مربوط بالنشر التلقائي حاليًا.
- مصدر التحديث الحالي: `origin/main` عند `1c97cfe` (Balanced app icon size)، مع تجهيزات الويب المحلية. يمكن التحقق من رقم المصدر المنشور عبر `/build-info.json`.
- نشر هذا التحديث: `dpl_7odVRmnAwaFvxpAsKSnV8E11RP3J`. أكد الرابط العام رقم المصدر `1c97cfe92f0a048a85dd2209f908a28f9ceb4872`.

أرسل لصاحب موقع التعريف: **ضع `https://riyal-web.vercel.app` وجهةً لزر Try Riyal.**

بعد نجاح النشر لا يحتاج التطبيق إلى بقاء Flutter أو VS Code أو جهازك قيد التشغيل.

اختبار المتصفح يشمل إنشاء حساب محلي جديد، وحفظ ميزانية 750 ريال لكل قسم، والصفحة الرئيسية بأحجام الهاتف والكمبيوتر والتابلت، وردًا فعليًا من Riyal Bot يستخدم الميزانية، وثبات `device_id` بعد تحديث الصفحة. ملفات الأسرار مستبعدة من حزمة النشر.

نجح هذا الاختبار محليًا وعلى الرابط العام بعد نشر `1c97cfe`، دون أخطاء تشغيل JavaScript. نجحت 98 حالة اختبار Flutter، وفحص التحليل، واختبارا خادم Gemini، وبناء الإنتاج. أعاد المساران `/.env` و`/assets/.env` الحالة 404 على الرابط العام.

قيد معروف في الاختبار: عند تفعيل طبقة إمكانية الوصول في Flutter قبل الانتقال بحركة العملة، اختفت حقول التسجيل من شجرة إمكانية الوصول وتعذر تركيزها في الاختبار. نجح التسجيل بالنقر والكتابة العادية دون هذه الطبقة؛ لم نغيّر حركة العملة أو ندّعِ اكتمال دعم قارئات الشاشة. يستخدم اختبار المتصفح النقر الفعلي في التسجيل والميزانيات، ثم يفعّل الطبقة لباقي المسار.

لتحديث المشروع الموجود لاحقًا، نفّذ من جذر مشروع Flutter:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\prepare-web.ps1 -Build
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\vercel.ps1 --prod --yes --scope riyal3
```

الأول يبني نسخة جديدة ويفحص الأسرار، والثاني ينشرها على المشروع نفسه مع بقاء الرابط الثابت. لا تحتاج إعادة إضافة مفتاح Gemini مع كل تحديث.

## ما الذي يتغير؟

- يحافظ الويب على نظام الحسابات المحلي في أحدث نسخة GitHub: يجب إنشاء الحساب أولًا على المتصفح نفسه، ثم تسجيل الدخول بكلمة مروره. هذا ليس تسجيل دخول Supabase، ولا تنقل حسابات الجوال إلى المتصفح تلقائيًا.
- يستقبل البوت سياق البيانات من التطبيق كما في أحدث نسخة الجوال، مع إبقاء المفتاح وتعليمات الخادم خارج تحكم العميل.
- سياسات Supabase الحالية لا تعزل بيانات كل زائر أمنيًا. لا تستخدم هذه النسخة لحفظ بيانات شخصية أو مالية حقيقية. لم نغير قاعدة البيانات.
- المتصفح يتصل بـ`/api/gemini` في نفس موقع التطبيق. مفتاح Gemini يبقى في وظيفة Vercel، وليس في ملفات Flutter.
- ملف `.env` المحلي لم يعد من أصول التطبيق. يحتوي `assets/config/public.env` المُولَّد على إعدادات عامة فقط.
- شاشة الويب تتوقف عن التمدد بعد عرض 1100 بكسل، وتبقى الشاشات الصغيرة وتخطيطات الجوال الأصلية كما هي.
- أُضيف خط صغير لرمز الريال فقط، مولّد من رسم SVG الموجود في المشروع، لمعالجة غياب الرمز في بعض خطوط المتصفح.

## تجهيز نسخة الإنتاج

افتح المشروع في VS Code، ثم من القائمة اختر **Terminal → New Terminal**. تأكد أن الطرفية هي PowerShell وأنك في مجلد المشروع:

```powershell
cd 'C:\FlutterBootcamp\Flutter start\Final Project\riyal'
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\prepare-web.ps1 -Build
```

الأمر يقرأ `.env` محليًا، وينسخ فقط رابط Supabase ومفتاحه العام وإعداد رابط الوسيط الاختياري. بعدها يشغل `flutter build web --release`، ويفحص الملفات بحثًا عن المفاتيح السرية، ويجهز:

- `build/web`: ملفات Flutter الإنتاجية.
- `build/vercel`: حزمة النشر، وتحتوي `public` لملفات Flutter و`api` لوظيفة Gemini وإعداد Vercel.

لا ترفع جذر المشروع أو ملف `.env`. لا تستخدم `--dart-define-from-file=.env` لبناء الويب؛ قيم البناء قابلة للاستخراج.

## أدوات Vercel والحساب

Vercel يستضيف التطبيق على الإنترنت ويعطيك رابط HTTPS. تحتاج حسابك الخاص فقط، ولا تحتاج حساب صديقك أو ربط GitHub للنشر الأول.

في هذا الجهاز جُهز Node.js وأداة Vercel داخل `.tools` بدل التثبيت العام. شغّل الأداة من جذر المشروع عبر:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\vercel.ps1 login
```

اتبع رابط تسجيل الدخول الذي تعرضه الأداة، وسجل بالحساب الجديد. قد تعرض الأداة رمز تحقق؛ أدخله فقط في صفحة Vercel الرسمية. لا ترسل الرمز أو كلمة المرور في المحادثة. انتظر ظهور رسالة نجاح الدخول.

بعدها نربط مجلد النشر بمشروع جديد:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\vercel.ps1 link
```

عند الأسئلة: وافق على إعداد المجلد، اختر حسابك الشخصي، واختر **No** عند السؤال عن ربط مشروع موجود. اكتب اسم المشروع المتفق عليه. إذا سأل عن مجلد الكود استخدم `./`. لا تختَر مشروع موقع التعريف.

إعدادات الحزمة تختار **Other**، ومجلد المخرجات `public`، ولا تنفذ بناء Flutter في Vercel: البناء تم محليًا بالفعل.

## إعداد مفتاح البوت على الخادم

في موقع Vercel افتح مشروع التطبيق الجديد ثم **Settings → Environment Variables**. أضف:

| الاسم | القيمة | المكان |
|---|---|---|
| `GEMINI_API_KEY` | مفتاحك الحالي من `.env`، دون علامات اقتباس | Production وPreview فقط؛ عيّنه Sensitive إن توفر الخيار |
| `GEMINI_MODEL` | `gemini-3.8-flash`، أو نموذج تحققنا من توفره | Production وPreview |

لا تحتاج خدمة Gemini إلى مفتاح Supabase سري. إعدادات Supabase العامة موجودة أصلًا في نسخة Flutter.

قبل مشاركة الرابط، افتح **Firewall → Configure → New Rule** وأضف حد طلبات للمسار `/api/gemini`، مثل 10 طلبات في الدقيقة لكل IP. راجع أي تسعير تعرضه Vercel قبل الموافقة. حد الطلبات داخل الكود احتياطي لكل نسخة خادم ولا يحل محل هذا الحد الموزع. الدخول التجريبي ليس مصادقة للخادم.

## النشر

بعد ضبط الحساب والمشروع والمتغيرات:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\vercel.ps1 --prod
```

هذا الأمر يرفع حزمة `build/vercel` فقط. انتظر اكتمال النشر وظهور رابط الإنتاج. افتح الرابط في نافذة خاصة للتأكد أنه متاح للزوار، ثم اختبر البوت والتنقل وحفظ البيانات بعد تحديث الصفحة. لا نفترض نجاح النشر قبل اختبار الرابط الفعلي.

الرابط الفعلي يظهر في نتيجة الأمر وفي صفحة مشروع Vercel ضمن **Domains**. هذا هو الرابط الذي يوضع في زر **Try Riyal** على موقع التعريف. لا ترسل رابط Preview مؤقتًا لصاحب موقع التعريف.

## التشغيل المحلي والجوال

بعد توليد الإعداد العام، يعمل `flutter run -d chrome` للواجهة؛ خادم Flutter وحده لا يشغل وظيفة Vercel. لاختبار إصدار الإنتاج مع البوت محليًا:

```powershell
.\.tools\node-v22.23.2-win-x64\node.exe .\scripts\preview-web.cjs
```

افتح `http://127.0.0.1:8080`. هذا الرابط محلي وليس الرابط الذي تشاركه مع الآخرين.

رابط `GEMINI_PROXY_URL` الفعلي هو `https://riyal-web.vercel.app/api/gemini`. أُضيف إلى `.env` المحلي وأُعيد توليد الإعداد العام؛ بذلك يستخدم تشغيل الجوال التالي الوسيط نفسه دون تضمين مفتاح سري. لا تستخدم `--dart-define-from-file=.env` لبناء إصدار موزع أو إصدار ويب.

## التحديثات القادمة

أعد تشغيل سكربت البناء ثم أمر النشر لتحديث نفس المشروع. السكربت يحتفظ بربط Vercel بين مرات البناء، لكن `flutter clean` يحذفه لأنه داخل `build`؛ بعد التنظيف استخدم `link` واختر مشروع التطبيق الموجود.

يمكن إعداد GitHub Actions لاحقًا لتثبيت Flutter وبناء التطبيق وتجهيز حزمة Vercel ونشرها عند الدفع إلى الفرع المتفق عليه. لا يكفي ربط GitHub وحده وافتراض أن Vercel سيبني Flutter تلقائيًا. لم نضف نشرًا آليًا أو نرفع أي commit.

## الملفات المهمة التي تغيرت

| الملفات | السبب |
|---|---|
| `pubspec.yaml` و`lib/main.dart` و`lib/data/supabase_config.dart` | تحميل الإعداد العام بدل الملف السري |
| `lib/services/gemini_api.dart` و`riyal_bot_config.dart` | الاتصال بالوسيط دون مفتاح داخل المتصفح |
| `api/gemini.js` | الاحتفاظ بمفتاح Gemini على الخادم والتحقق من حجم الطلب ونوعه ومحتواه |
| `lib/widgets/web_app_frame.dart` | ضبط عرض الويب مع إبقاء الجوال كما هو |
| `lib/theme/app_theme.dart` و`app_typography.dart` وخط `RiyalSymbol` | إظهار رمز العملة من رسم المشروع نفسه |
| `web/index.html` و`manifest.json` وأيقونات ريال | عنوان الصفحة وألوانها وشعارها |
| `vercel.json` و`scripts/prepare-web.ps1` و`vercel.ps1` | بناء آمن وحزمة نشر مستقلة وأداة أوامر محلية |
| `scripts/preview-web.cjs` و`browser-smoke.cjs` والاختبارات الجديدة | اختبار الخادم والمتصفح وعدم إرسال المفاتيح |
| `.env.example` و`.gitignore` | توضيح إعدادات الخادم وتجاهل ملفات الأدوات والإعدادات المولدة |

عند سحب آخر تحديثات GitHub احتُفظ بـ`README.md` الأحدث من المستودع. النسخة المحلية السابقة محفوظة في `.tools/README-before-github-pull.md` وفي stash باسم `Riyal web deployment and pre-existing README before updating main`. لم نطبّق تغييرات على قاعدة البيانات أو ندفع شيئًا إلى GitHub. تحققنا قراءةً فقط من أن مبالغ migration 0014 مطابقة للبيانات التجريبية الحالية.
